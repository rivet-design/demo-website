import { motion } from 'motion/react';

const Logo = () => {
  return (
    <motion.div
      className="flex items-center font-cta text-4xl text-primary"
      style={{ width: 88.4141, height: 65 }}
    >
      rivet
    </motion.div>
  );
};

export default Logo;
